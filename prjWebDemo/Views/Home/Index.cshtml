@{
    ViewData["Title"] = "Home Page";
}
@section Styles{
    <style>
        .display-4 {
            color: saddlebrown;
        }
    </style>
}
    <div class="text-center">
        <h1 class="display-4">Welcome</h1>
        @*@Html.Partial("Partial")*@
        <p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
    </div>
    <div>
        <ul id="ul"></ul>
    </div>
    @section Scripts{
    <script>
        var emp = {
            "employees": [
                { "name": "Tom", "workYears": 3, "salary": 35000 },
                { "name": "Jack", "workYears": 5, "salary": 40000 },
                { "name": "Mary", "workYears": 7, "salary": 45000 }
            ]
        };
        const getUl = document.getElementById("ul");

        //使用forEach迴圈
       emp.employees.forEach((value,index,array)=>{
            const { name, workYears, salary } = value;
            getUl.innerHTML +=  `<li>${name} - ${salary}</li>`;

            console.log(name);
            console.log(salary);
        })

        //使用map()迴圈
        let datas = emp.employees.map((emp,index,array)=> {
            const { name, workYears, salary } = emp;
            
        return `<li>${name} - ${salary}</li>`;
        })
        console.log(datas);
        getUl.innerHTML = datas.join("");


        //for(let i =0;i < emp.employees.length;i++)
        //{
        
        //    //console.log(emp.employees[i].name);
        //    //console.log(emp.employees[i].workYears);
        //    //console.log(emp.employees[i].salary);
        //    console.log(name)

        //    let setli = document.createElement("li");
        //    let liText = document.createTextNode(name);

        //    setli.appendChild(liText);
        //    getUl.appendChild(setli);
            
            
        //}


    </script>
}
